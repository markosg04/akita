# Fast-recursion delivery source boundary

This branch carries the measured companion's net inline/trusted-setup source delta
(28fc72021 -> 99fb06aa4) atop public Blake f5f75335. Public norm-table and canonical
FoldDraw APIs are retained. Historical full-proof measurements used Jolt f2e97d10 / Akita99fb06aa; the
saved-artifact replay used Jolt c2372a0a with that same companion. Neither is a
new measurement of this branch.

All Jolt Git dependencies pin c2372a0a9ffd2b37768e501b90037c146664757d, the immutable
source anchor at https://github.com/a16z/jolt, planned ref
refs/tags/recursion-source-c2372a0a. It supplies the exact measured jolt-field,
jolt-inlines-blake2 digest adapter, and jolt-inlines-ntt source. The old72dc6451
revision did not contain the NTT package. The anchor's complete Jolt workspace
retains historical local patches; it is a leaf-source anchor, not a standalone
Jolt delivery checkout. Cargo consumer roots must ignore dependency-workspace
patch tables; metadata validation must establish that no sibling paths leak.

The final Jolt consumer pins this companion through markosg04/akita only and
patches those three Git leaf packages to its own identical workspace members,
which yields one jolt-field owner. Package edges stop at the shared leaves:
Akita does not depend on the Jolt prover through these normal dependencies.
The excluded legacy recursion profile also aligns its direct Jolt SDK/macro and
field pins to this anchor to avoid duplicate field revisions; its build/runtime
compatibility requires separate validation and is not measured by the Jolt
recursion runner evidence.

Publication is staged only after review: expose the exact anchor object/ref,
then this immutable companion revision, then the final Jolt revision containing
that fork pin. No Git ref or Cargo lock resolution is claimed public before the
actual publication/metadata checks. Root, fuzz, and excluded-profile lockfiles were regenerated by offline Cargo
metadata using isolated local Git transport mirrors; locked metadata checks
confirm one jolt-field and no akita-field or shake package. This is local graph
validation, not a public-fetch or compiler result. Focused inline tests and saved
proof replay remain later gates, not a repeat full proof. Historical ELF/proof/
binary artifacts remain unchanged.

The excluded profile needs its own Git-source patches: the historical Jolt SDK
anchor otherwise imports pre-Blake LayerZero Akita and a second field revision.
Its patch table selects markosg04/akita source ancestor
2bceddba86e07971bbd0538e46060df3caf3031b. All library sources/manifests are identical
to this delivery; subsequent changes are locks/profile delivery only. Pinning an
ancestor avoids a self-referential commit hash. The profile has local Akita
packages for native artifact/guest glue and distinct Git Akita packages beneath
the Jolt SDK. A shared field identity does not make those Akita types identical;
compiler/runtime compatibility remains an explicit gate. No path overrides are
used to hide that distinction.
